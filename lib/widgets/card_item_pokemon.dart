import 'package:flutter/material.dart';

class CardItemPokemon extends StatelessWidget {
  const CardItemPokemon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
